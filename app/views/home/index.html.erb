<h1>멋사 강의 목록</h1>

    <% if Lecture.all.exists? %>
        <% @lectures.each do |l| %>
            <p><%= l.title %></p>
        <% end %>
        
    <% else %>
        <h2>강의 목록이 없습니다!</h2>
    <% end%>